query charactersQuery($page: Int!, $filter: FilterCharacter!) {
  characters(page: $page, filter: $filter) {
    info {
      count
      pages
    }
    results {
      id
      name
      image
      species
      status
      type
    }
  }
}